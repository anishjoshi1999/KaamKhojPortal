<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>You are searching for <%=type%></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body class="bg-light">
    <div class="container py-5">
        <h2 class="text-primary text-center">You are searching for <%=type.toUpperCase()%> Provider</h2>

        <!-- Create an ordered list with button-like links -->
        <ol class="list-group list-group-numbered">
            <% jobsData.forEach((element) => { %>
                <li class="list-group-item">
                    <a href="/<%=type%>/<%= element.name %>" >
                        <%= element.name %>: <%= element.totalAvailableJobs %>
                    </a>
                </li>
            <% }) %>
        </ol>
    </div>
</body>
</html>
